{% extends 'base.html.twig' %}

{% block title %}
	{{ dresseur.username }}
{% endblock %}

{% block body %}
	<div class="container mx-auto">
	<div class="text-white text-center col-5 bg-accueil  fw-bolder border-success border-4 border rounded-pill mx-auto mb-2">
					<h1>Profil de
						{{ dresseur.username }}</h1>
				</div>
		<div class="row">
			
				
				<div class="col-md-5 text-danger bg-light text-primary text-center my-3 border rounded-4 mx-auto">
				<h2>Mes équipes
				</h2>
				<ul class="list-unstyled">
					{% for team in teams %}
						<li>{{ team.name }}</li>
						{# ou tout autre champ de l'entité Team #}
					{% else %}
						<li>Aucune équipe trouvée.</li>
					{% endfor %}
				</ul>
			</tbody>
		</table>
{# 
		<a href="{{ path('app_dresseur_index') }}">back to list</a>

		<a href="{{ path('app_dresseur_edit', {'id': dresseur.id}) }}">edit</a> #}
		{# {{ include('dresseur/_delete_form.html.twig') }} #}

	</div>
</div></div>{% endblock %}
