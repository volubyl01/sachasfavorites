{% extends 'base.html.twig' %}

{% block title %}Element
{% endblock %}

{% block body %}

	<div class="container-fluid">
		<div class="row">
			
				<div class="col-3 bg-accueil text-light  border-success border-4 border rounded-pill mx-auto mb-2">
					<h1 class="display-5 text-center fw-bolder">{{ element.specificite }}</h1>
				</div>
				<div class="text-center my-5">
				<img src="{{ asset('uploads/Image/' ~ element.illustration) }}" class="img-fluid" alt="{{ element.specificite }}" height="200px" width="200px"></div>
				<div class="text-center">
					<a href="{{ path('app_element_index') }}">back to list</a>

					<a href="{{ path('app_element_edit', {'id': element.id}) }}">edit</a>

					{{ include('element/_delete_form.html.twig') }}
				</div>

			
		</div>
	</div>
{% endblock %}
