<nav class="navbar navbar-expand-lg bg-body-transparent">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <img src="{{asset('build/images/PW_logo.webp') }}" width="120" height="auto" class="d-inline-block align-top" alt="Brand">
        </a>
        <button class="navbar-toggler bg-danger" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto text-center">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">
                        <h5 class="text-danger">Home</h5>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_a_propos') }}">
                        <h5 class="text-danger">A propos</h5>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_team_index') }}">
                        <h5 class="text-danger">Les Teams</h5>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_element_index') }}">
                        <h5 class="text-danger">Les énergies</h5>
                    </a>
                </li>

                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">
                            <h5 class="text-danger">Déconnexion</h5>
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">
                            <h5 class="text-danger">Connexion</h5>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">
                            <h5 class="text-danger">Inscription</h5>
                        </a>
                    </li>
                {% endif %}

                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_dresseur_show', {'id': app.user.id}) }}">
                            <h5 class="text-danger">Mon profil</h5>
                        </a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_youtube_search', {'query': 'terme de recherche'}) }}">
                                <h5 class="text-danger">Rechercher sur YouTube</h5>
                            </a>
                        </li>
                    {% endif %}
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
